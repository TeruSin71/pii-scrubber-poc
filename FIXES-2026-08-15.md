# Fixes applied 2026-08-15 — reconcile against the deployment plan

For the VS Code agent executing
`docs/superpowers/plans/2026-08-15-pii-scrubber-aicore-deployment.md`.
All three defects that plan identified are now **fixed at source** in this
drop. Regression-tested: `python test_fixes.py` — 15/15 pass, and
`/v1/selftest` re-verified at `recall_pct 100.0`, `45/45`, `over_detections 6`
on the current code (so the previously "stale" 6 is now a *verified* number,
not a documented leftover).

## What changed

| # | Plan finding | Fix | File |
|---|---|---|---|
| 1 | `allowlist.txt` never copied into the image — production silently runs the 27-token seed | Added to the `COPY` line | `Dockerfile` |
| 2 | Miner's `TOKEN # count=N` lines survive `_load_allowlist()` as inert 46-char tokens | Loader now strips **inline** comments (`line.split('#',1)[0]`) | `app.py` |
| 3 | All-caps USER_ID collision: a real user ID `KLEIN`/`BRAUN` is byte-identical to an allowlistable token; recall gate can't see it | Context backstop: pure-alpha suppressible tokens are **not** suppressed within ±40 chars of user-context words ("posted by", "user", "author"…). Unambiguous shapes (underscore/digit//NS/) suppress unconditionally | `app.py` |
| — | Docs stale (`CUSTOM_OBJECT_RULE` undocumented; "three ways" now four; over-detections unverified) | README config table + README-DEPLOY updated | both READMEs |

New file: `test_fixes.py` — one regression test per defect class plus the
recall invariant. `samples.json` untouched (still 13/45).

## Plan impacts — what to re-sequence

- **Task 2 Step 6 (packaging decision): RESOLVED — Option A is already
  applied.** The Dockerfile ships the allowlist. The step collapses to
  verifying the `COPY` line; no approval-gated edit remains.
- **Task 2 Step 3b comment-strip workaround: now optional.** The `sed
  's/#.*//'` merge still works, but the loader tolerates raw candidate lines.
  **Human review of mined candidates remains mandatory** — defect 3's
  backstop reduces, but does not eliminate, the leak risk of allowlisting a
  real user ID (context words won't always be within the window).
- **Task 1 Step 5 probe: unchanged.** `ZSD_REBATE_CALC` and
  `/SOVOSD/RFYTXDISPLAY` stay cleartext even near the word "Author" —
  explicitly regression-tested (3c).
- **Task 3 Step 5: expect Option A output** — `Allowlist loaded: <N> tokens`,
  never the seed fallback.
- **Open finding 2 (all-caps collision): downgraded, not closed.** The
  backstop is an automated control where there was none; the residual case
  (a collision token appearing with *no* user context nearby) still rests on
  the mandatory review pass. Open findings 1 (docs) is closed; 3
  (`ES_SD_REBATE`, no Z/Y prefix) is unchanged — `TADIR` remains its fix.
- Suggested addition to Task 1: run `python test_fixes.py` alongside the
  selftest — it costs seconds and pins all three defect classes.

## Unchanged invariants

`samples.json` (13/45, byte-identical) · `requirements.txt` · recall 100.0 ·
no outbound calls at inference · `ZY_SURNAME_GUARD` · all approval gates and
hard rules in the plan.
